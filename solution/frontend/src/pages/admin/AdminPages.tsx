import { useEffect, useState } from 'react'
import { Download, Edit3, Lock, Plus, Search, Shield, Unlock } from 'lucide-react'
import type { Role, User } from '../../domain'
import { ROLE_LABELS } from '../../domain'
import { adminService } from '../../services/mock'
import AppLayout from '../../layouts/AppLayout'
import { Badge, Btn, Card, EmptyState, Input, Modal, PageHeader, Select, StatCard } from '../../components/shared'
import { DemoStateBoundary } from '../SystemPages'

export function AdminDashboard() { return <DemoStateBoundary title="Tổng quan hệ thống"><AppLayout pageTitle="Tổng quan hệ thống"><PageHeader title="Tổng quan hệ thống" subtitle="Theo dõi người dùng, request và hoạt động quản trị" /><div className="grid grid-cols-4 gap-4 mb-5"><StatCard label="Tổng người dùng" value="342" color="blue" /><StatCard label="Đang hoạt động" value="287" color="green" /><StatCard label="Request hôm nay" value="1,247" color="purple" /><StatCard label="Lỗi hệ thống" value="2" color="red" /></div><div className="grid grid-cols-4 gap-4 mb-5"><StatCard label="Customer" value="215" color="gray" /><StatCard label="Bank Employee" value="124" color="gray" /><StatCard label="Knowledge Manager" value="8" color="gray" /><StatCard label="Tài khoản bị khóa" value="8" color="amber" /></div><div className="grid grid-cols-3 gap-5"><Card className="col-span-2"><div className="p-4 border-b font-semibold text-sm">Hoạt động quản trị gần đây</div>{['Tạo người dùng mới · Nguyễn Minh Anh', 'Khóa tài khoản · Bùi Thanh Tú', 'Cập nhật ma trận quyền · System Admin', 'Export audit log · Admin Hệ thống'].map((item, index) => <div key={item} className="p-4 border-b text-xs flex justify-between"><span>{item}</span><span className="text-gray-400">{index + 1} giờ trước</span></div>)}</Card><Card><div className="p-4 border-b font-semibold text-sm">Cảnh báo hệ thống</div>{['2 request API thất bại', '3 tài liệu cần re-index', 'Backup dữ liệu hoàn tất'].map(item => <div key={item} className="p-4 border-b text-xs">{item}</div>)}</Card></div></AppLayout></DemoStateBoundary> }

const roles: Role[] = ['customer', 'bank_employee', 'knowledge_manager', 'system_admin']
export function AdminUsers() { const [users, setUsers] = useState<Omit<User, 'password'>[]>([]); const [query, setQuery] = useState(''); const [role, setRole] = useState(''); const [status, setStatus] = useState(''); const [editing, setEditing] = useState<Omit<User, 'password'> | null>(null); const [create, setCreate] = useState(false); useEffect(() => { adminService.users().then(setUsers) }, []); const filtered = users.filter(user => (!query || `${user.name} ${user.email} ${user.username}`.toLowerCase().includes(query.toLowerCase())) && (!role || user.role === role) && (!status || user.status === status)); const save = async (value: Omit<User, 'password'>) => { const next = { ...value, password: 'password' }; const saved = await adminService.saveUser(next); setUsers(await adminService.users()); setEditing(null); setCreate(false); return saved }; const toggle = (user: Omit<User, 'password'>) => void save({ ...user, status: user.status === 'active' ? 'locked' : 'active' }); return <DemoStateBoundary title="Người dùng"><AppLayout pageTitle="Người dùng"><PageHeader title="Quản lý người dùng" subtitle={`${filtered.length} người dùng`} actions={<Btn onClick={() => setCreate(true)}><Plus size={14} /> Tạo người dùng</Btn>} /><Card className="p-4 mb-4"><div className="flex gap-2"><div className="relative flex-1"><Search size={15} className="absolute left-3 top-2.5 text-gray-400" /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Tìm tên, username hoặc email..." className="w-full border rounded pl-9 py-2 text-xs" /></div><Select value={role} onChange={setRole} options={[{ value: '', label: 'Tất cả vai trò' }, ...roles.map(value => ({ value, label: ROLE_LABELS[value] }))]} /><Select value={status} onChange={setStatus} options={[{ value: '', label: 'Tất cả trạng thái' }, { value: 'active', label: 'Đang hoạt động' }, { value: 'locked', label: 'Bị khóa' }]} /></div></Card><Card>{filtered.length === 0 ? <EmptyState title="Không có người dùng" /> : <div className="overflow-x-auto"><table className="w-full text-xs"><thead className="bg-gray-50"><tr>{['Họ tên', 'Username', 'Email', 'Vai trò', 'Trạng thái', 'Ngày tạo', 'Đăng nhập cuối', 'Thao tác'].map(item => <th key={item} className="text-left p-3">{item}</th>)}</tr></thead><tbody>{filtered.map(user => <tr key={user.id} className="border-t"><td className="p-3 font-medium">{user.name}</td><td className="p-3 font-mono">{user.username}</td><td className="p-3">{user.email}</td><td className="p-3"><Badge variant={user.role === 'system_admin' ? 'info' : 'active'} label={ROLE_LABELS[user.role]} /></td><td className="p-3"><Badge variant={user.status} /></td><td className="p-3">{user.createdAt}</td><td className="p-3">{user.lastLogin || '—'}</td><td className="p-3"><div className="flex gap-1"><button onClick={() => setEditing(user)} className="p-1.5 hover:bg-gray-100" title="Chỉnh sửa"><Edit3 size={14} /></button><button onClick={() => toggle(user)} className="p-1.5 hover:bg-gray-100" title={user.status === 'active' ? 'Khóa' : 'Mở khóa'}>{user.status === 'active' ? <Lock size={14} /> : <Unlock size={14} />}</button></div></td></tr>)}</tbody></table></div>}</Card><UserModal user={editing} create={create} onClose={() => { setEditing(null); setCreate(false) }} onSave={value => void save(value)} /></AppLayout></DemoStateBoundary> }

function UserModal({ user, create, onClose, onSave }: { user: Omit<User, 'password'> | null; create: boolean; onClose: () => void; onSave: (user: Omit<User, 'password'>) => void }) { const [value, setValue] = useState<Omit<User, 'password'>>(user || { id: crypto.randomUUID(), username: '', name: '', email: '', role: 'customer', status: 'active', createdAt: new Date().toLocaleDateString('vi-VN') }); useEffect(() => { if (user) setValue(user) }, [user]); const open = create || !!user; return <Modal open={open} onClose={onClose} title={create ? 'Tạo người dùng mới' : 'Chỉnh sửa người dùng'} footer={<><Btn variant="outline" onClick={onClose}>Hủy</Btn><Btn disabled={!value.name || !value.username || !value.email} onClick={() => onSave(value)}>Lưu</Btn></>}><div className="space-y-3"><Input label="Họ tên" required value={value.name} onChange={name => setValue(v => ({ ...v, name }))} /><Input label="Username" required value={value.username} onChange={username => setValue(v => ({ ...v, username }))} /><Input label="Email" type="email" required value={value.email} onChange={email => setValue(v => ({ ...v, email }))} /><Select label="Vai trò" value={value.role} onChange={role => setValue(v => ({ ...v, role: role as Role }))} options={roles.map(role => ({ value: role, label: ROLE_LABELS[role] }))} /><Select label="Trạng thái" value={value.status} onChange={status => setValue(v => ({ ...v, status: status as 'active' | 'locked' }))} options={[{ value: 'active', label: 'Đang hoạt động' }, { value: 'locked', label: 'Bị khóa' }]} /></div></Modal> }

const PERMISSIONS = ['chat:public', 'chat:internal', 'documents:read', 'documents:manage', 'metadata:manage', 'relations:manage', 'reindex:manage', 'users:manage', 'roles:manage', 'audit:read']
export function AdminRoles() { const [matrix, setMatrix] = useState<Record<Role, Set<string>>>(() => ({ customer: new Set(['chat:public']), bank_employee: new Set(['chat:internal', 'documents:read']), knowledge_manager: new Set(['documents:read', 'documents:manage', 'metadata:manage', 'relations:manage', 'reindex:manage']), system_admin: new Set(['users:manage', 'roles:manage', 'audit:read']) })); const toggle = (role: Role, permission: string) => setMatrix(value => { const next = { ...value, [role]: new Set(value[role]) }; next[role].has(permission) ? next[role].delete(permission) : next[role].add(permission); return next }); return <DemoStateBoundary title="Vai trò và quyền"><AppLayout pageTitle="Vai trò và quyền"><PageHeader title="Quản lý vai trò và quyền" subtitle="Ma trận quyền truy cập theo role" /><div className="grid grid-cols-4 gap-4 mb-5">{roles.map(role => <Card key={role} className="p-4"><Shield className="text-[#C8102E]" size={20} /><div className="font-semibold text-sm mt-3">{ROLE_LABELS[role]}</div><div className="text-xs text-gray-500 mt-1">{matrix[role].size} permission đang bật</div></Card>)}</div><Card><div className="overflow-x-auto"><table className="w-full text-xs"><thead className="bg-gray-50"><tr><th className="text-left p-3">Permission</th>{roles.map(role => <th key={role} className="p-3 text-center">{ROLE_LABELS[role]}</th>)}</tr></thead><tbody>{PERMISSIONS.map(permission => <tr key={permission} className="border-t"><td className="p-3 font-mono">{permission}</td>{roles.map(role => <td key={role} className="p-3 text-center"><input type="checkbox" checked={matrix[role].has(permission)} onChange={() => toggle(role, permission)} /></td>)}</tr>)}</tbody></table></div></Card></AppLayout></DemoStateBoundary> }

export function AdminAudit() { const [logs, setLogs] = useState<Awaited<ReturnType<typeof adminService.audit>>>([]); const [query, setQuery] = useState(''); const [result, setResult] = useState(''); const [detail, setDetail] = useState<typeof logs[number] | null>(null); useEffect(() => { adminService.audit().then(setLogs) }, []); const filtered = logs.filter(log => (!query || `${log.actor} ${log.action} ${log.target} ${log.requestId}`.toLowerCase().includes(query.toLowerCase())) && (!result || log.result === result)); const exportCsv = () => { const csv = ['time,actor,role,action,target,result,requestId', ...filtered.map(log => [log.time, log.actor, log.role, log.action, log.target, log.result, log.requestId].join(','))].join('\n'); const url = URL.createObjectURL(new Blob([csv], { type: 'text/csv' })); const anchor = document.createElement('a'); anchor.href = url; anchor.download = 'audit-log.csv'; anchor.click(); URL.revokeObjectURL(url) }; return <DemoStateBoundary title="Nhật ký hệ thống"><AppLayout pageTitle="Nhật ký hệ thống"><PageHeader title="Nhật ký hệ thống" subtitle="Theo dõi toàn bộ hoạt động trong hệ thống" actions={<Btn variant="outline" onClick={exportCsv}><Download size={14} /> Xuất CSV</Btn>} /><Card className="p-4 mb-4"><div className="flex gap-2"><div className="relative flex-1"><Search size={15} className="absolute left-3 top-2.5 text-gray-400" /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Actor, action, resource hoặc request ID..." className="w-full border rounded pl-9 py-2 text-xs" /></div><Select options={[{ value: '', label: 'Tất cả vai trò' }, ...roles.map(role => ({ value: role, label: ROLE_LABELS[role] }))]} /><Select value={result} onChange={setResult} options={[{ value: '', label: 'Tất cả kết quả' }, { value: 'success', label: 'Thành công' }, { value: 'failed', label: 'Thất bại' }]} /><Input type="date" /></div></Card><Card><table className="w-full text-xs"><thead className="bg-gray-50"><tr>{['Thời gian', 'Người thực hiện', 'Vai trò', 'Hành động', 'Loại tài nguyên', 'Đối tượng', 'Kết quả', 'Request ID', ''].map(item => <th key={item} className="text-left p-3">{item}</th>)}</tr></thead><tbody>{filtered.map(log => <tr key={log.id} className="border-t"><td className="p-3 font-mono">{log.time}</td><td className="p-3">{log.actor}</td><td className="p-3">{ROLE_LABELS[log.role]}</td><td className="p-3 font-mono">{log.action}</td><td className="p-3">{log.resourceType}</td><td className="p-3">{log.target}</td><td className="p-3"><Badge variant={log.result === 'success' ? 'completed' : 'failed'} /></td><td className="p-3 font-mono">{log.requestId}</td><td className="p-3"><button onClick={() => setDetail(log)} className="text-[#C8102E]">Chi tiết</button></td></tr>)}</tbody></table></Card><Modal open={!!detail} onClose={() => setDetail(null)} title="Chi tiết audit log" footer={<Btn variant="outline" onClick={() => setDetail(null)}>Đóng</Btn>}>{detail && <div className="text-xs space-y-3"><div><b>Request ID:</b> {detail.requestId}</div><div><b>Dữ liệu trước:</b><pre className="bg-gray-50 p-3 mt-1 rounded">{detail.before || 'Không có'}</pre></div><div><b>Dữ liệu sau:</b><pre className="bg-gray-50 p-3 mt-1 rounded">{detail.after || 'Không có'}</pre></div></div>}</Modal></AppLayout></DemoStateBoundary> }
